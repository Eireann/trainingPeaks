define(
[
    "jquery",
    "backbone",
    "shared/views/userSettingsView"
],
function ($, Backbone, UserSettingsView)
{
    describe("UserSettings View", function ()
    {
        beforeEach(function ()
        {
        });

        it("", function()
        {

        });

    });
});
