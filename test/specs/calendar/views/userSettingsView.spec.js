// use requirejs() instead of define() here, to keep jasmine test runner happy
requirejs(
[
    "jquery",
    "backbone",
    "views/userSettings/userSettingsView"
],
function ($, Backbone, UserSettingsView)
{
    describe("UserSettings View", function ()
    {
        beforeEach(function ()
        {
        });

        it("", function()
        {

        });

    });
});